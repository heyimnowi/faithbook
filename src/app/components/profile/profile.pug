.column.m-right-3.flex-3.profile-container
  .profile-section.event-detail-header.column
    .row.row-top.card.m-bottom-5
      .profile-pic-big.flex-1
        img(src="{{profileCtrl.user.image}}")
      .column.flex-3
        .row.row-space-between.row-top
          h1.title-regular.m-bottom-1
            | {{ profileCtrl.user.name }}. 24
          button.green-button(ng-click='profileCtrl.sendRequest()' ng-class='{ "request-button-sent": profileCtrl.requestSent}')
            span(ng-if='!profileCtrl.requestSent')
              | Send request
            span(ng-if='profileCtrl.requestSent')
              | Request sent
        .row.m-bottom-3
          img.m-right-1(src="assets/pin.svg")
          span.event-location
            | {{ profileCtrl.user.city }}
        span.religion-title.m-bottom-1
          | {{ profileCtrl.user.religion }}
        .row.interest-wrapper.m-bottom-3
          span.interest(ng-repeat='interest in profileCtrl.user.interests')
            | # {{ interest }}
        p.body-light
          | {{ profileCtrl.user.bio }}
    .card.colum.column-center.testimonials-section(settings="profileCtrl.slickConfig")
      h1.title-regular.m-bottom-1
        | Testimonials
      slick(autoplay='true')
        .testimonial-wrapper
          img(src="https://caritasinternationalis.files.wordpress.com/2013/02/jordanvol.jpg" alt="")
          span.name
            | Amira Al Hassan
          span.review
            | "This local was very helpful, we had a great conversation over coffee. I learned a lot about their religion and traditions, and also shared some of my experiences with Islam. Would definitely recommend meeting them!"
        .testimonial-wrapper
          img(src="https://avatars1.githubusercontent.com/u/7938095?s=460&v=4" alt="")
          span.name
            | Noelia Lopez
          span.review
            | "We had a really great time walking through Rome, seeing some of the religious sights, and finding out more about their background. I promised I would show them my city too if they ever visited!"
